{% extends "base.html" %}

{% load grade_tags %}

{% block content %}
    
    {% if user.is_authenticated %}
        <p>Logged as {{ user}}&nbsp;|&nbsp;<a href="{% url twitterauth:logout %}">Logout</a>
    {% else %}
        <p><a href="{% url twitterauth:login %}">Login</a></p>
    {% endif %}
    
    <div>
        {% for day in days %}
            <div data-role="collapsible-set">
              <h1>{{ day|date:"d \d\e F \d\e Y" }}</h1>
                {% for hour in hours %}
                         <h3>{{ hour }}:00</h2>
                    {% for talk in talks %}
      <div data-role="collapsible" data-collapsed="true">
        <h3> {% display_talks day hour %} <span class="ui-li-count">09:00 &mdash; 10:00</span></h3>
        <p>Sum&aacute;rio:</p>
        <p>Palestrante:</p>
        <p><a href="talk.html">Mais informações:</a></p>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
{% endblock %}
